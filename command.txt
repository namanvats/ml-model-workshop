source venv/bin/activate
pip install --upgrade pip
pip install pandas scikit-learn dvc
pip freeze > requirements.txt
git add .
git commit -m "initial project setup"
git status


iris (data) --> preparation --> output proccessed data 


Adding train stage in dvc
dvc stage add \
> -n train \
> -d src/train_model.py \
> -d data/processed/train.csv -d data/processed/test.csv \
> -o models/iris_logistic_regression.pkl \ 
> -M metrics/metrics.json \
> python src/train_model.py 